# Gitleaks allowlist. The env files contain only public-by-design frontend
# config (Auth0 SPA client id, Stripe publishable key) — see docs/build-channels.md.
[allowlist]
paths = ['''env/.*\.env(\.example)?$''']
